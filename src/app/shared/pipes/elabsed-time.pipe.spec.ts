import {ElabsedTimePipe} from './elabsed-time.pipe';

describe('ElabsedTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ElabsedTimePipe();
    expect(pipe).toBeTruthy();
  });
});
